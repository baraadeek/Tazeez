const ProfileStyle = (theme) => ({
  editProfile: {
    textTransform: "capitalize",
    display: "inline-block",
    fontSize: "15px",
    fontWeight: 500,
    padding: "8px 24px",
    borderRadius: "4px",
  },
});

export default ProfileStyle;
