import { createStyles, makeStyles, Theme } from '@material-ui/core';

const useAssignedQuestionnaireViewerStyles = makeStyles((theme: Theme) =>
createStyles({}));

export default useAssignedQuestionnaireViewerStyles