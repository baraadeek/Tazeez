import { createStyles, makeStyles, Theme } from '@material-ui/core';

const useAssignedQuestionnairesListStyles = makeStyles((theme: Theme) =>
createStyles({}));

export default useAssignedQuestionnairesListStyles